# Copy to .env.local (never commit .env.local)

# Anthropic (Thought Untangler, Chat, optional Haiku pre-check)
ANTHROPIC_API_KEY=

# Supabase (journal — add when Phase 2 starts)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Single-user passphrase gate (hashed in app — add when Phase 2 starts)
AMIGO_PASSPHRASE=
